<%= form_tag protests_path, method: :get do %>
  <%= select_tag :search_station, options_for_select(Station.all.map { |station| [station.name, station.id] }.unshift(["Select Station", ""]), selected: params[:search_station]), class: "search_station" %>
  <br>
  <%= select_tag :search_category, options_for_select(AssaultCategory.all.map { |station| [station.name, station.id] }.unshift(["Select Category", ""]), selected: params[:search_category]), class: "search_category" %>
  <br>
  <%= date_field_tag :start_date, params[:start_date], class: "search_date" %>
  <br>
  <%= submit_tag "Search", class: "btn see-results animate__animated animate__pulse animate__slower animate__infinite" %>
  <br>
  <%= link_to "clear filters", "/protests?", class: "restore-link" %>
<% end %>

